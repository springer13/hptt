get_filename_component(HPTT_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

list(APPEND CMAKE_MODULE_PATH ${HPTT_CMAKE_DIR})
list(REMOVE_AT CMAKE_MODULE_PATH -1)

if(NOT TARGET HPTT::hptt)
    include("${HPTT_CMAKE_DIR}/HPTTTargets.cmake")
endif()

set(HPTT_LIBRARIES HPTT::hptt)
